# Mission 1 Archive TODO

- Recover Dune dashboard/query metadata and CSV/JSON exports once Dune API or UI access is available.
- Run a contiguous archive-node `eth_getLogs` pass over the full verified block range.
- Reconcile Dog #200 final/open-auction state beyond latest `auction()` result (current archive observes Dog 200 created with amount 0 and unsettled at latest block).
- Reconcile full WETH → Idle → idleWETH → treasury/stream flows.
- Reconcile Ukraine/Unchain donation flows, including Dog #1 100% donation and later 10% donations.
- Decode Superfluid stream/IDA/CFA events if feasible.
- Decide whether small generated Mission 1 JSON files should be copied to `public/generated/mission1/` for future dashboard use.
- Keep Mission 3 live dashboard UI untouched unless explicitly requested.
